# Matriz de Remediación de Auditoría — Paquete v0.2.2

**Estado:** `EN AUDITORÍA EXTERNA`  
**Fecha:** 2026-09-15  
**Paquete Auditado Previamente:** `politica-canon-v0.2.1.zip` (`NO PASS`)  
**Paquete Remediado:** `politica-canon-v0.2.2.zip`  

---

## 1. Tabla de Trazabilidad y Cierre de Bloqueos Críticos (C-01 a C-10)

| ID Hallazgo | Descripción del Bloqueo | Archivo Modificado | Sección / Ubicación | Prueba de Cierre / Remediación Aplicada |
|---|---|---|---|---|
| **C-01** | Contrato de autorización finalizaba en `allowed: true` sin validar rol | [`docs/security/AUTHORIZATION_MATRIX.md`](docs/security/AUTHORIZATION_MATRIX.md) | Sección 4, `evaluateAuthorizationContract()` | Reescrito el evaluador en patrón incondicional **Default-Deny**. Comprueba concesión explícita de rol, aislamiento de organización, membresía activa de workspace, asignación a objeto, clasificación, especialidad de revisor, estado del recurso, ausencia de conflicto de interés y MFA reciente antes de otorgar `allowed = true`. Separadas las acciones `SUBMIT_FOR_REVIEW` de `ISSUE_REVIEW`. |
| **C-02** | Ausencia de modelo para asignación de roles sensibles y coautores | [`docs/03_ROLES_Y_PERMISOS.md`](docs/03_ROLES_Y_PERMISOS.md), [`docs/architecture/ERD.md`](docs/architecture/ERD.md) | Tablas `role_assignments` y `role_assignment_requests` | Creado el modelo de doble control para asignación de roles sensibles (`APPROVER`, `PUBLISHER`, `ADMIN`) exigiendo dos aprobaciones independientes (`role_assignment_requests`). Incorporadas membresías múltiples y coautoría en borradores. |
| **C-03** | Incoherencia entre ERD conceptual (25+ entidades) y esquema físico de Fase 1 | [`docs/architecture/ERD.md`](docs/architecture/ERD.md) | Sección 1, 2 y 3 | Dividido el ERD en: (1) Modelo ERD Conceptual Completo y (2) Esquema Físico Exacto de la Fase 1. Definidas absolutamente todas las tablas del esquema físico (incluyendo `invitations`, `user_sessions`, `role_assignments`, `role_assignment_requests`, `authority_bodies`, `authority_memberships`, `audit_events`, `audit_outbox`). Separadas las tablas `comment_threads` y `comments`. |
| **C-04** | Aislamiento multi-tenant solo formulado en prosa sin DDL ni FKs compuestas | [`docs/architecture/ERD.md`](docs/architecture/ERD.md) | Sección 4, DDL de Aislamiento | Especificadas cláusulas DDL PostgreSQL declarando `UNIQUE (organization_id, id)` y `FOREIGN KEY (organization_id, workspace_id) REFERENCES workspaces (organization_id, id)`. Creada suite de pruebas SQL con consultas negativas cross-tenant. |
| **C-05** | Contradicciones en el ciclo de vida e inmutabilidad de dictámenes/publicaciones | [`docs/04_FLUJO_EDITORIAL.md`](docs/04_FLUJO_EDITORIAL.md), [`docs/architecture/ERD.md`](docs/architecture/ERD.md) | Sección 1 y 2, `working_drafts` y `publication_events` | Los dictámenes (`reviews`) permanecen estrictamente inmutables; cuando se modifica un borrador, se inserta una nueva `DocumentVersion` y los dictámenes anteriores se marcan como obsoletos mediante eventos. Las publicaciones son inmutables; los retiros se registran en `publication_events`. Añadidos `base_version_id` y `revision_number` en `working_drafts` para OCC. |
| **C-06** | Igualdad de hashes imposible entre content_hash JSON y artifact_hash PDF | [`docs/04_FLUJO_EDITORIAL.md`](docs/04_FLUJO_EDITORIAL.md), [`docs/architecture/ERD.md`](docs/architecture/ERD.md) | Sección 3, `publications` | Eliminada la exigencia de igualdad entre `content_hash` y `artifact_hash`. Se registran ambos hashes por separado en `publications` junto con `document_version_id`, `decision_id`, `template_version`, `renderer_version`, `render_params` y `artifact_key`. |
| **C-07** | Auditoría y outbox no ejecutables como estaban descritas | [`docs/adr/ADR-0003-audit-logging-and-outbox.md`](docs/adr/ADR-0003-audit-logging-and-outbox.md), [`docs/architecture/ERD.md`](docs/architecture/ERD.md) | `ADR-0003`, `audit_outbox` | Creado `ADR-0003` especificando rol DB de mínimo privilegio sin permisos `UPDATE/DELETE`, trigger `prevent_modification_or_deletion()`, patrón `audit_outbox` atómico en la misma transacción DB y cadena hash particionada por `organization_id`. |
| **C-08** | Enlaces rotos y atribución de responsabilidad a la IA | [`docs/00_INDICE_CANONICO_DOCUMENTAL.md`](docs/00_INDICE_CANONICO_DOCUMENTAL.md), [`docs/13_REGISTRO_DECISIONES.md`](docs/13_REGISTRO_DECISIONES.md), [`docs/adr/ADR-0001-architecture.md`](docs/adr/ADR-0001-architecture.md) | Todo el paquete | Corregidos los 20 enlaces relativos (0 enlaces rotos verificados por script). Retirada la IA de toda atribución de autoridad en decisiones/ADR. Actualizados changelog y estados a v0.2.2. |
| **C-09** | Publicación no enlazada a decisiones ni reproducible | [`docs/architecture/ERD.md`](docs/architecture/ERD.md), [`docs/security/AUTHORIZATION_MATRIX.md`](docs/security/AUTHORIZATION_MATRIX.md) | Tabla `publications` y `publication_events` | Añadidos los campos `decision_id`, `template_version`, `renderer_version`, `render_params_json`, `artifact_key`, `format` en `publications`. |
| **C-10** | Criterios imposibles en el backlog de la Fase 1 | [`docs/backlog/PHASE_1_BACKLOG.md`](docs/backlog/PHASE_1_BACKLOG.md) | Historias 1.1 a 10.1 | Acotado el backlog exclusivamente al alcance de la fundación técnica. Reemplazado "0 vulnerabilidades npm" por una política de riesgo operable (CVSS, SLAs de parches, excepciones autorizadas). Añadidas historias para backup codes MFA, CSRF, fijación de sesión y expiración de invitaciones. |

---

## 2. Tabla de Remediación de Hallazgos de Severidad Alta (H-01 a H-05)

| ID Hallazgo | Descripción del Hallazgo | Archivo Modificado | Sección / Ubicación | Prueba de Cierre / Remediación Aplicada |
|---|---|---|---|---|
| **H-01** | Estrategia de sesión no decidida | [`docs/adr/ADR-0002-session-management.md`](docs/adr/ADR-0002-session-management.md) | `ADR-0002` completo | Creado `ADR-0002` adoptando cookies de sesión opaca revocables (`HttpOnly`, `Secure`, `SameSite=Strict`) en Redis/DB. Detallada rotación en login/MFA/cambio de rol, expiración inactiva/absoluta, CSRF y cierre global. |
| **H-02** | Threat model sobreestimaba garantías | [`docs/security/THREAT_MODEL.md`](docs/security/THREAT_MODEL.md) | Secciones 1, 2 y 3 | Distinguido riesgo inherente de riesgo residual objetivo. Añadidas amenazas de invitaciones/correo, recuperación de cuenta, gestión de secretos, backups, proveedores de IA e insiders con autoridad. |
| **H-03** | Regresión de alcance en el PRD | [`docs/01_PRD.md`](docs/01_PRD.md) | Secciones 1, 2 y 3 | Restaurados todos los requisitos originales del PRD (editor rico, comentarios, historial/diff/restauración, búsqueda, dashboards, notificaciones, exportaciones detalladas, historias críticas). |
| **H-04** | Imprecisión terminológica en firma e inmutabilidad | Todo el paquete | Múltiples secciones | Sustituidos términos imprecisos. Usada terminología exacta: "decisión registrada por autoridad autenticada", "hash de integridad", "firma criptográfica" (solo si existe) y "base de datos append-only defensiva". |
| **H-05** | Operaciones de backup dentro de la aplicación web | [`docs/product/SCREEN_AND_FLOW_MAP.md`](docs/product/SCREEN_AND_FLOW_MAP.md), [`docs/backlog/PHASE_1_BACKLOG.md`](docs/backlog/PHASE_1_BACKLOG.md) | `SCR-23`, Gate 9 | Eliminada la capacidad de restauración desde la UI web (se conserva solo monitor de salud). Las restauraciones quedan como procedimientos operativos out-of-band por CLI. |
